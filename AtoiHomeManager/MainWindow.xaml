<Controls:MetroWindow  x:Class="AtoiHomeManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"                       
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AtoiHomeManager"
        Title="AtoiHomeManager" Height="1080" Width="800" 
        BorderThickness="0" 
        Background="Black"
        Closing="Window_Closing" 
        WindowStartupLocation="Manual" 
        Left="2000" Top="0"
        Loaded="Window_Loaded" 
        ResizeMode="CanResizeWithGrip" 
        ShowInTaskbar="True"
        LocationChanged="Window_LocationChanged">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
            </ResourceDictionary.MergedDictionaries>
            <!--Button Style-->
            <Button x:Key="MainButton" Width="50"  Height="50"/>
            <Style TargetType="Button">
                <Setter Property="HorizontalAlignment" Value="Left"/>
                <Setter Property="VerticalAlignment" Value="Top"/>
                <Setter Property="Width" Value="40" />
                <Setter Property="Height" Value="40" />
                <Setter Property="Foreground"  Value="Black"/>
                <Setter Property="Background"  Value="White"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Controls:MetroWindow.RightWindowCommands>
        <Controls:WindowCommands>
            <Button Content="settings" />
            <Button>
                <StackPanel Orientation="Horizontal">
                    <Rectangle Width="20"
                   Height="20"
                   Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}">
                        <Rectangle.OpacityMask>
                            <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_cupcake}" />
                        </Rectangle.OpacityMask>
                    </Rectangle>
                    <TextBlock Margin="4 0 0 0"
                   VerticalAlignment="Center"
                   Text="deploy cupcakes" />
                </StackPanel>
            </Button>
        </Controls:WindowCommands>
    </Controls:MetroWindow.RightWindowCommands>


    <Grid x:Name="gridMain" Margin="0,0,54,0" Background="Black" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--Content-->
        <Grid Grid.Row="1" HorizontalAlignment="left" Width="792">
            <Button x:Name="buttonConnect" ToolTip="Connect to service"  Margin="10,0,0,0" 
                    Click="ButtonConnect_Click">
                <Button.Content>
                    <Image Width="30" Height="30" Opacity="1">
                        <Image.Source>
                            <BitmapImage 
                            UriSource="pack://application:,,,/atoihomemanager;component/Resources/Images/connect.png">
                            </BitmapImage>
                        </Image.Source>
                    </Image>
                </Button.Content>
            </Button>

            <Button x:Name="buttonDownloadImage" ToolTip="Draw selected image"  Margin="50,0,0,0" 
                    Click="ButtonDownloadImage_Click">
                <Rectangle Width="30" Height="30" 
                     Fill="{Binding Foreground, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Button}}}">
                    <Rectangle.OpacityMask>
                        <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_image}" />
                    </Rectangle.OpacityMask>
                </Rectangle>
            </Button>

            <Button x:Name="buttonImageRotation" ToolTip="Rotate image" Margin="90,0,0,0"
                    Click="ButtonImageRotation_Click">
                <Rectangle Width="30" Height="30" 
                     Fill="{Binding Foreground, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Button}}}">
                    <Rectangle.OpacityMask>
                        <VisualBrush Stretch="Fill" Visual="{StaticResource appbar_transform_rotate_clockwise}" />
                    </Rectangle.OpacityMask>
                </Rectangle>
            </Button>

            <Button x:Name="buttonViewMode" ToolTip="Chang view mode" Margin="130,0,0,0"
                    Click="ButtonViewMode_Click">
                <Button.Content>
                    <Image Width="20" Height="20" Opacity="1">
                        <Image.Source>
                            <BitmapImage 
                            UriSource="pack://application:,,,/atoihomemanager;component/Resources/Images/scrollview24.png">
                            </BitmapImage>
                        </Image.Source>
                    </Image>
                </Button.Content>
            </Button>

            <Slider x:Name="slider" Grid.Column="0" Orientation="Vertical" HorizontalAlignment="Left" Minimum="1" Width="0"/>
            <ScrollViewer Name="scrollViewer" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Visible" 
                          VerticalAlignment="Top"
                          Margin="5,50,0,0" Background="Black">
                <ScrollViewer.Style>
                    <Style TargetType="{x:Type ScrollViewer}">
                        <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
                        <Setter Property="VerticalScrollBarVisibility" Value="Disabled"/>
                    </Style>
                </ScrollViewer.Style>
                <Grid Name="gridImage" Width="720" Height="1280"  RenderTransformOrigin="0.5,0.5" Background="Black">
                    <Grid.LayoutTransform>
                        <TransformGroup>
                            <ScaleTransform x:Name="scaleTransform"/>
                        </TransformGroup>
                    </Grid.LayoutTransform>
                    <Image  x:Name="imageDownload" RenderTransformOrigin="0.5, 0.5" />
                </Grid>
            </ScrollViewer>
        </Grid>
    </Grid>
</Controls:MetroWindow>

